module ReqLLM
  # Forward-declared empty stub so Message/Context compile before Unit D.
  # Unit D (plan Task 7) reopens this struct with id/name/arguments/metadata
  # plus to_wire/from_wire helpers.
  struct ToolCall
  end
end
