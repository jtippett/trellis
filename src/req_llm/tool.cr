module ReqLLM
  # Forward-declared empty stub so Context compiles before Unit D.
  # Unit D (plan Task 7) reopens this struct with name/description/
  # parameter_schema/callback plus to_json_schema.
  struct Tool
  end
end
